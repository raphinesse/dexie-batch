{
  "name": "dexie-batch",
  "version": "0.2.0",
  "description": "Fetch DB entries in batches to improve performance while respecting IPC size constraints",
  "main": "dexie-batch.js",
  "scripts": {
    "format": "prettier --no-semi --single-quote --trailing-comma es5 --write '*.js'",
    "test": "browserify spec.js | tape-run | tap-spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/raphinesse/dexie-batch.git"
  },
  "keywords": [
    "dexie",
    "get",
    "fetch",
    "batch",
    "bulk"
  ],
  "author": "Raphael von der Grün",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/raphinesse/dexie-batch/issues"
  },
  "homepage": "https://github.com/raphinesse/dexie-batch#readme",
  "peerDependencies": {
    "dexie": "^2.0.0-beta.11"
  },
  "devDependencies": {
    "blue-tape": "^1.0.0",
    "browserify": "^14.4.0",
    "dexie": "^2.0.0-beta.11",
    "prettier": "^1.5.3",
    "tap-spec": "^4.1.1",
    "tape-run": "^3.0.0"
  }
}
